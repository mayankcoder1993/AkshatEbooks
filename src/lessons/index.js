import { lesson01 } from './lesson01.jsx'

/**
 * The lesson registry. When a course transcript is provided, each
 * lesson from the transcript becomes one entry here:
 *
 *   { id, icon, title, shortTitle, subtitle, tags, Body }
 */
const lessons = [lesson01]

export default lessons
