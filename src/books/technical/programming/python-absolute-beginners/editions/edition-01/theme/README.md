# Edition Theme Overrides

The edition currently uses publisher tokens from `src/publishing/design-tokens.js` and the technical profile stylesheet. Add only edition-specific token or CSS overrides here; do not duplicate shared styles.
