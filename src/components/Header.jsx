export default function Header({brand,theme,onToggleTheme,onHome,lessons,active,onSelect,quickStart,onQuickStart,onBookPreview,onSavePdf,onSaveWord,exporting}) {
  return <header className="site-header"><div className="header-inner"><button className="brand brand-button" onClick={onHome} title="Back to book library"><span className="brand-text"><strong>{brand.imprint}</strong><span>{brand.tagline}</span></span></button><nav className="lesson-chips" aria-label="Chapters">{quickStart && <button className={active===-1?'chip active':'chip'} onClick={onQuickStart}><span className="chip-num">QS</span>Quick Start</button>}{lessons.map((lesson,index)=><button key={lesson.id} className={index===active?'chip active':'chip'} onClick={()=>onSelect(index)}><span className="chip-num">{String(index+1).padStart(2,'0')}</span>{lesson.shortTitle}</button>)}</nav><div className="actions"><button className="icon-btn" onClick={onToggleTheme} title="Toggle theme">{theme==='light'?'🌙':'☀️'}</button><button className="btn" onClick={onHome}>⌂ Library</button><button className="btn" onClick={onBookPreview}>📖 Book preview</button><button className="btn" onClick={onSaveWord} disabled={exporting}>{exporting?'Preparing…':'📄 Word'}</button><button className="btn primary" onClick={onSavePdf}>🖨 PDF</button></div></div></header>
}
