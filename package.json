{
  "name": "akshat-ebooks-notes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A single-source interactive ebook system for absolute beginners.",
  "scripts": {
    "predev": "npm run generate:docx && npm run build:single",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "generate:docx": "node --import ./scripts/register-jpg.mjs scripts/generate-docx.mjs",
    "build:single": "vite build --config vite.singlefile.config.mjs && cp single-build/index.html public/AkshatEBooks-Interactive-Book.html",
    "generate:crest": "node scripts/generate-crest-variants.mjs"
  },
  "dependencies": {
    "docx": "^9.5.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
